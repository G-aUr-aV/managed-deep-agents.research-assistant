[project]
name = "research-assistant-preview"
version = "0.0.0"
description = "A Managed Deep Agent project."
requires-python = "==3.12.3"
dependencies = [
    "managed-deepagents==0.7.3",
    "tavily-python>=0.7.27",
    "langchain-openai==1.6.0",
    "deepagents==0.7.10",
]
