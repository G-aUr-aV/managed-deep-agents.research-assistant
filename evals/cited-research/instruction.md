Research the original Transformer paper ("Attention Is All You Need" by Vaswani et al.).

Write a brief of at least a few paragraphs to `/app/answer.txt`. The brief must:

- Summarize the architecture (self-attention vs recurrence) in your own words.
- Use `paper_search` to find the paper and include at least one paper URL or DOI from the result.
- Include at least one other web citation from `internet_search` (a full `http` or `https` URL).

Do not invent paper URLs. Use the tools. Put working notes in the sandbox if useful, but the graded file is `/app/answer.txt`.
